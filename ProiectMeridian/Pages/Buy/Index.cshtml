@page
@model ProiectMeridian.Pages.Buy.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Buying[0].Client)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Buying[0].Telefon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Buying[0].DateofBuiying)
            </th>
            <th>
            <th>
                    @Html.DisplayNameFor(model =>model.Buying[0].Telefon.Producator)
            </th>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Buying) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Client.FullName)
            </td>
                <td>
                    @Html.DisplayFor(modelItem =>item.Telefon.Nume)
                </td>
                <td>
                    @Html.DisplayFor(modelItem =>item.Telefon.Producator.FullName)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateofBuiying)
            </td>
                 <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
